# Build artifacts
*.o
*.a
*.so

# Compiled binaries (keep source only)
rkmpi-encoder/rkmpi_enc
rkmpi-encoder/v4l2_query
fb-status/fb_status
fb-status/fb_status_arm
h264-streamer/29-h264-streamer/rkmpi_enc

# Large SDK libraries (use printer libs at runtime)
rkmpi-encoder/lib/
rkmpi-encoder/openssl-1.1.1w/
rkmpi-encoder/openssl-1.1.1w.tar.gz
# Note: include/, lib-printer/, openssl-arm/ are tracked for CI builds (~22MB total)

# Python cache
__pycache__/
*.pyc

# Editor files
*.swp
*.swo
*~
.vscode/

# Claude workspace
.claude/

# Temporary files
*.tmp
*.log
*.bak
